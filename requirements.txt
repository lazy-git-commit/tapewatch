# News ingestion
requests==2.34.2

# Sentiment classification.
# PINNED BELOW 1.0.0 ON PURPOSE. anthropic 1.0.0 removed `temperature` from
# messages.create and accepts no **kwargs, so news/fetcher.py's temperature=0
# raises TypeError before any request — stopping the pipeline at its entry
# point. Every test mocks the client, so CI cannot see it. See the note in
# .github/dependabot.yml before changing this.
anthropic==0.54.0

# Offline classifier comparison only (analysis/qwen_eval.py). Alibaba Model
# Studio speaks the OpenAI protocol, so the OpenAI SDK is the client for it.
# The live trading path does NOT import this.
openai==3.3.1

# Market data & price confirmation
yfinance==1.3.0
pandas==2.3.2
pandas_market_calendars==5.4.0

# Order execution — Trading 212 REST API called directly via requests

# Scheduling
APScheduler==3.10.4

# Storage
psycopg2-binary==2.9.12

# Utilities
python-dotenv==1.2.3
pytz==2026.3.post1
