# News ingestion
requests==2.32.3

# Sentiment classification
anthropic==0.54.0

# Offline classifier comparison only (analysis/qwen_eval.py). Alibaba Model
# Studio speaks the OpenAI protocol, so the OpenAI SDK is the client for it.
# The live trading path does NOT import this.
openai==1.109.1

# Market data & price confirmation
yfinance==1.3.0
pandas==2.3.2
pandas_market_calendars==5.4.0

# Order execution — Trading 212 REST API called directly via requests

# Scheduling
APScheduler==3.10.4

# Storage
psycopg2-binary==2.9.12

# Utilities
python-dotenv==1.0.1
pytz==2024.2
